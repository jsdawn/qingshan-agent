# AI Agent Monorepo 项目规则

## 🎯 项目概览

- **名称**: ai-agent-monorepo
- **类型**: TurboRepo Monorepo（前端 + 后端 + 共享模块）
- **技术栈**: TypeScript、React 18、Express、Vercel AI SDK、DeepSeek API
- **包管理器**: pnpm (workspace)

## 📁 项目结构

```
ai-agent-monorepo/
├── apps/server          # 后端 Express 应用
├── apps/web             # 前端 React 应用
├── packages/shared      # 共享模块
├── docs/developer       # 开发文档（按日期组织）
└── 配置文件
```

## 🔤 代码规范

### 语言要求

- **所有代码注释必须使用中文**
- 代码本身使用英文（变量名、函数名等遵循编程规范）
- 提交信息支持中英混合，优先使用中文

### 命名规范

- **变量/函数**: camelCase（例：`messageCount`, `handleSubmit`）
- **类/接口**: PascalCase（例：`ChatMessage`, `ApiResponse`）
- **常量**: UPPER_SNAKE_CASE（例：`MAX_TOKENS`, `API_BASE_URL`）
- **文件**:
  - 普通文件: camelCase（例：`chatMessage.ts`）
  - 入口文件: `index.ts`
  - 配置文件: 原名（例：`vite.config.ts`）

### TypeScript 规范

- **必须为所有变量、参数、返回值标注类型，禁止使用 `any`**
- 使用 `interface` 定义复杂对象类型
- 使用 `type` 定义联合类型或类型别名
- 异步函数返回值必须标注为 `Promise<Type>`

### 注释规范

- 代码规范注释
- 所有注释必须使用中文
- **生成或修改的代码，函数、方法、类、接口及关键逻辑说明，必须使用符合 JSDoc 规范的中文注释**
- 函数注释使用 JSDoc 格式：
  ```typescript
  /**
   * 函数功能简述
   *
   * @param arg1 参数说明
   * @param arg2 参数说明
   * @returns 返回值说明
   */
  ```
- 关键逻辑添加行内注释，使用 `//` 说明
- 代码块注释使用 `/* */`

## 📦 模块架构

### 后端 (apps/server)

- **入口**: `src/index.ts`
  - Express 服务器配置
  - 路由定义
  - CORS 和中间件设置
  - AI 集成逻辑
- **类型**: `src/types.ts`
  - 后端特定类型定义
  - 扩展共享类型

### 前端 (apps/web)

- **主组件**: `src/App.tsx`
  - React 聊天界面
  - 使用 `@ai-sdk/react` 的 `useChat` 钩子
  - 流式消息显示
- **样式**: `src/App.css`
  - CSS3 样式
  - 响应式设计
  - 变量管理

### 共享模块 (packages/shared)

- **类型**: `src/types/index.ts`
  - `ChatMessage`: 聊天消息
  - `ChatRequest`: 请求类型
  - `ChatResponse`: 响应类型
- **工具**: `src/utils/index.ts`
  - `AI_CONFIG`: AI 配置常量
  - 消息格式化函数
  - ID 生成函数

## 🔧 开发工作流程

### 添加新功能的步骤

1. **定义类型**（如需）：在 `packages/shared/src/types/` 定义
2. **实现逻辑**：编写功能代码，添加中文注释
3. **测试**：确保功能正常运行
4. **更新文档**：在 `docs/developer/YYYY-MM-DD/` 下记录

### 创建新的 API 端点

1. 在 `packages/shared/src/types/` 定义请求/响应类型
2. 在 `apps/server/src/index.ts` 实现端点
3. 在前端调用该端点
4. 更新 API 文档

### 扩展 AI 能力

- **工具调用**: 在后端 `streamText` 中添加 `tools` 参数
- **系统提示**: 通过 `systemPrompt` 字段自定义
- **模型切换**: 修改 `AI_CONFIG.MODEL` 常量

## 📝 AI 文档产物规则

### 临时文档产物存放位置

- **路径**: `docs/developer/YYYY-MM-DD/`
- **文件名**: 描述性名称，使用 kebab-case
- **格式**: Markdown (.md)

### 文档类型

- **README**: 项目/模块说明
- **QUICKSTART**: 快速启动指南
- **DEVELOPMENT**: 详细开发指南
- **API**: API 文档
- **DEPLOYMENT**: 部署指南

### 文档要求

- 所有文本必须使用中文
- 包含完整的代码示例
- 提供问题排查指南
- 包含相关链接

## ✅ 代码质量检查清单

在提交代码前，确保：

- [ ] 所有注释都是中文
- [ ] 没有使用 `any` 类型
- [ ] 函数都有 JSDoc 注释
- [ ] AI 新增或修改的代码已补充 JSDoc 规范的中文注释
- [ ] 代码格式统一（缩进、命名等）
- [ ] 没有未使用的导入或变量
- [ ] 错误处理完善
- [ ] 关键逻辑有中文注释

## 🚀 常用命令

```bash
# 安装依赖
pnpm install

# 启动开发环境（同时启动前后端）
pnpm dev

# 仅启动后端
pnpm --filter @ai-agent/server dev

# 仅启动前端
pnpm --filter @ai-agent/web dev

# 构建所有项目
pnpm build

# 清理输出目录
pnpm clean
```

## 🔐 环境配置

### 必需的环境变量

- `DEEPSEEK_API_KEY`: DeepSeek API 密钥（后端）
- `REACT_APP_API_URL`: 后端 API 地址（前端，可选）

### 可选的环境变量

- `PORT`: 后端服务器端口（默认 3000）
- `NODE_ENV`: 运行环境（development/production）
- `FRONTEND_URL`: 前端 URL（CORS 配置）

## 🔗 重要文件路径

| 文件                           | 说明                    |
| ------------------------------ | ----------------------- |
| `apps/server/src/index.ts`     | 后端主文件              |
| `apps/web/src/App.tsx`         | 前端主组件              |
| `packages/shared/src/index.ts` | 共享模块导出            |
| `turbo.json`                   | TurboRepo 配置          |
| `package.json`                 | 根目录配置（workspace） |
| `.env.example`                 | 环境变量示例            |

## 📚 相关资源

- [TurboRepo 文档](https://turbo.build/docs)
- [Vercel AI SDK](https://sdk.vercel.ai)
- [DeepSeek API](https://platform.deepseek.com)
- [React 官方文档](https://react.dev)
- [Express 文档](https://expressjs.com)

## 🎓 最佳实践

1. **保持模块独立**: 各模块有明确的职责边界
2. **使用共享模块**: 前后端通用代码放在 `packages/shared`
3. **类型优先**: 先定义类型再实现逻辑
4. **错误处理**: 所有异步操作都应有错误处理
5. **文档优先**: 在实现前编写 API 设计文档
6. **测试驱动**: 重要功能配套单元测试
7. **按日期组织**: 临时开发文档产物按日期放入 `docs/developer`

---

**更新时间**: 2026-04-07
**维护者**: AI Agent Team
