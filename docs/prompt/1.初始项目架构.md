请生成一个完整的、可直接运行的 TurboRepo Monorepo 架构 AI Agent 初始项目，严格遵循以下要求，所有代码可复制即用，无需额外修改，仅需填入 API Key 即可启动，全程使用免费额度工具和最优技术栈，贴合企业级最佳实践。

一、项目基础配置（必严格遵循）

1.  工具与包管理器：使用 TurboRepo 作为 Monorepo 工具，pnpm 作为包管理器（所有依赖用 pnpm 安装，配置正确的 workspace）
2.  项目名称：ai-agent-monorepo
3.  整体架构：Monorepo 结构，包含 apps（前端+后端）和 packages（共享模块），所有代码均为 TypeScript 编写，注释完整、结构清晰
4.  免费 AI 服务：优先使用 DeepSeek API（兼容 OpenAI 格式，免费额度高），无需使用付费模型，确保代码中适配 DeepSeek 接口
5.  核心依赖：Vercel AI SDK（AI 交互核心）、LangChain.js（预留 Agent 扩展能力）、React 18、Express、TypeScript

二、项目结构（必须严格按照此结构生成，不新增、不遗漏文件夹/文件）
ai-agent-monorepo/
├── apps/ # 应用目录（前端+后端）
│ ├── web/ # 前端 React 项目（Vite + TS）
│ │ ├── public/ # 前端静态资源
│ │ ├── src/
│ │ │ ├── App.tsx # 聊天主界面（带流式打字机效果）
│ │ │ ├── main.tsx # 前端入口文件
│ │ │ └── vite-env.d.ts
│ │ ├── .env.example # 前端环境变量示例（空模板）
│ │ ├── index.html # 前端入口 HTML
│ │ ├── package.json # 前端依赖配置
│ │ ├── tsconfig.json # 前端 TS 配置
│ │ └── vite.config.ts # Vite 配置（适配 React + TS）
│ └── server/ # 后端 Node.js 项目（Express + TS）
│ ├── src/
│ │ ├── index.ts # 后端入口（启动服务、配置路由、AI 接口）
│ │ └── types.ts # 后端 TS 类型定义（接口、请求响应类型）
│ ├── .env.example # 后端环境变量示例（含 DEEPSEEK_API_KEY 模板）
│ ├── package.json # 后端依赖配置
│ └── tsconfig.json # 后端 TS 配置
├── packages/ # 共享模块（前后端通用）
│ └── shared/
│ ├── src/
│ │ ├── types/ # 前后端共享 TS 类型（聊天消息、AI 响应等）
│ │ │ └── index.ts
│ │ └── utils/ # 前后端共享工具函数（AI 配置、格式处理）
│ │ └── index.ts
│ ├── package.json # 共享模块依赖配置
│ └── tsconfig.json # 共享模块 TS 配置
├── .gitignore # Git 忽略文件（node_modules、.env 等）
├── package.json # 根目录依赖配置（pnpm workspace、turbo 脚本）
└── turbo.json # TurboRepo 配置（启动脚本、缓存配置）

三、各模块具体功能与代码要求（必实现，不简化）

### 1. 根目录配置

- package.json：配置 pnpm workspace（关联 apps 和 packages），添加 turbo 相关脚本（dev、build、start），指定包管理器为 pnpm
- turbo.json：配置 dev 脚本（同时启动前端 web 和后端 server），开启缓存，指定前后端启动命令
- .gitignore：忽略 node_modules、.env、dist、build 等无关文件

### 2. 共享模块（packages/shared）

- 共享类型：定义 ChatMessage 类型（包含 id、role、content）、AIStreamResponse 类型，供前后端复用
- 共享工具：封装 AI 基础配置（DeepSeek 基础 URL、默认模型），封装消息格式处理函数（统一前后端消息格式）
- 配置正确的 package.json，确保前后端可正常引入共享模块（设置正确的 main、types 入口）

### 3. 后端项目（apps/server）

- 核心功能：启动 Express 服务，配置 CORS（解决跨域），实现 /api/chat 接口（支持流式 SSE 输出）
- AI 集成：使用 Vercel AI SDK 的 streamText 方法，对接 DeepSeek API（兼容 OpenAI 格式），支持流式返回（stream: true）
- 错误处理：完善接口错误捕获、超时处理、API Key 校验，返回友好错误信息
- 类型支持：使用共享模块的类型，定义请求（req.body 包含 messages）和响应类型
- 配置：通过 dotenv 读取环境变量（DEEPSEEK_API_KEY），.env.example 包含清晰注释
- package.json：配置 dev 脚本（ts-node-dev 热更新）、build 脚本（tsc 编译），依赖版本兼容、无冗余

### 4. 前端项目（apps/web）

- 核心功能：React 聊天界面，实现输入框、发送按钮、聊天记录展示，支持流式打字机效果（逐字显示 AI 回复）
- AI 集成：使用 @ai-sdk/react 的 useChat 钩子，对接后端 /api/chat 接口，自动管理聊天历史、输入状态
- UI 设计：简洁美观，适配桌面端，聊天记录区分用户和 AI 角色（不同样式），输入框固定在底部
- 类型支持：引入共享模块的 ChatMessage 类型，确保类型安全
- 配置：Vite 配置适配 React + TS，支持环境变量（可配置后端接口地址），.env.example 包含接口地址模板
- package.json：配置 dev、build、preview 脚本，依赖版本与后端、共享模块兼容

### 5. 关键技术要求

- 流式输出：后端接口支持 SSE 流式传输，前端实现打字机效果（无需额外插件，使用 @ai-sdk/react 原生能力）
- AI 适配：DeepSeek API 配置正确（baseURL 设为 https://api.deepseek.com/v1，模型使用 deepseek-chat）
- 类型安全：所有代码严格使用 TypeScript，无 any 类型，接口、函数、变量均有明确类型定义
- 可运行性：所有代码可直接复制，安装依赖后即可启动，无需修改逻辑，仅需在 .env 中填入 DeepSeek API Key
- 扩展性：预留 Agent 扩展能力（后端预留工具调用、RAG 相关代码注释，前端预留扩展按钮）

四、额外要求（必满足）

1.  所有文件代码完整，无省略（无需用 ... 代替代码），注释清晰（关键逻辑、配置、接口均有注释）
2.  依赖版本选择稳定、兼容的最新版本，优先使用官方推荐版本，避免依赖冲突
3.  生成后给出详细的启动步骤（从安装依赖到启动项目，每一步命令清晰，适配 pnpm + turbo）
4.  给出环境变量配置说明（告诉如何获取 DeepSeek API Key，如何配置 .env 文件）
5.  确保项目启动后，前端可正常发送消息，后端返回流式 AI 回复，聊天历史正常保存
6.  不引入任何付费依赖、冗余依赖，所有依赖均有免费额度或完全免费
7.  遵循企业级代码规范，代码缩进、命名规范统一（驼峰命名，组件/函数命名清晰）

五、最终交付物（Copilot 需生成的内容）

1.  完整的项目目录结构（所有文件夹、文件），每个文件的完整代码
2.  每个文件的代码注释（关键逻辑、配置说明）
3.  详细的启动教程（步骤清晰，包含 pnpm 安装、依赖安装、环境变量配置、启动命令）
4.  简单的功能测试说明（如何测试流式聊天、如何排查启动问题）

目标：用户复制所有代码，按照启动步骤操作，填入 DeepSeek API Key 后，可直接启动前后端，实现完整的流式 AI 聊天功能，为后续 AI Agent 开发（工具调用、RAG、记忆系统）打下基础，项目架构符合企业级标准，可直接用于学习、面试或二次开发。
